---
import { LINKS } from '../_globals';

let {styled='white'} = Astro.props;
---
<a
	href={`${LINKS.WhatsApp}?text=Hola,+Quiero+agendar+una+cita!`}
	target="_blank"
	rel="noopener"
	aria-label="WhatsApp"
	class={
		styled === 'white' ?
			'text-white border border-white font-bold inline-block py-2 px-4 mt-4 rounded-full hover:bg-white hover:text-blue' :
			'text-blue border border-blue font-bold inline-block py-2 px-4 mt-4 rounded-full hover:bg-blue hover:text-white'
	}
>
	Agendá tu cita
</a>